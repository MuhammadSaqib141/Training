# Use the Alpine base image
FROM alpine:latest

# Install Docker CLI
RUN apk add --no-cache docker-cli awscli

# Set the entrypoint to a shell (you can modify this based on your needs)
ENTRYPOINT ["/bin/sh"]

ENTRYPOINT ["aws"]
